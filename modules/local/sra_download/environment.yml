name: sra_download
channels:
  - conda-forge
  - bioconda
dependencies:
  # 3.4.x segfaults on some systems; pin to the last known-good release
  # that matches the container image.
  - sra-tools=3.1.1
  - pigz
